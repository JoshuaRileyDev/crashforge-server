services:
  postgres:
    image: postgres:16
    container_name: crashforge-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: crash_reporter
      POSTGRES_USER: crash
      POSTGRES_PASSWORD: crash
    ports:
      - "${POSTGRES_HOST_PORT:-56432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crash -d crash_reporter"]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - pg-data:/var/lib/postgresql/data

  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crashforge-server
    restart: unless-stopped
    environment:
      PORT: 3000
      DATA_DIR: /app/data
      DATABASE_URL: ${DATABASE_URL:-postgres://crash:crash@postgres:5432/crash_reporter}
      WEBHOOK_URL: ${WEBHOOK_URL:-}
      WEBHOOK_AUTH_HEADER: ${WEBHOOK_AUTH_HEADER:-}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - app-data:/app/data

volumes:
  pg-data:
  app-data:
